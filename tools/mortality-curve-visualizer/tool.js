/* Mortality-Curve Visualizer — Tier 1 (client-side, Pyodide candidate).
 * In build. See /docs/tools-architecture.md and /tools/_template/tool.js for the contract.
 */
(function () {
  'use strict';
  // TODO: load bundled sample (age/exposure/deaths), fit Gompertz-Makeham and a GLM spline,
  // overlay observed qx vs. both fits (+ residuals) into #tool-root.
})();
